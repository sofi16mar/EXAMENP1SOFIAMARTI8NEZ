//se desea desarrollar un programa que me pérmita registar personas con el nombre, edad y estado civil y que permita buscar por estado civil y por edad, maximo 5 personas, menu 1, ingresar personas, 2 buscar estado civil, 3 buscar por edad, 4 otra opcion, 1 si, 2.no
#include <stdio.h>
#include <string.h>
#include "funciones.h"

int main (int argc, char *argv[]) {
    switch (Buscarnombre)
    {
    case 1:
        printf("¿Qué nombre desea buscar?");
        fgets (nombres, [50], stdin);
        int stramp(int Ingresaredad, char Ingresarnombre, char estadocivil);
        printf(%c,nombres );

        break;
    
    case 2:
        printf("¿Qué edad desea buscar?");
        fgets (edad, [50], stdin);
        int stramp(int Ingresaredad, char Ingresarnombre, char estadocivil);
        printf(%d,edad );

        break;
    case 3:
        printf("¿Qué estado civil desea buscar?");
        fgets (estadocivil, [50], stdin);
        int stramp(int Ingresaredad, char Ingresarnombre, char estadocivil);
        printf(%c,estadocivil );

        break;
    
    default:
        break;
    }
   

    return 0;
}


